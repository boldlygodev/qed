[package]
name = "qed-core"
version = "0.2.0"
edition = "2024"
description = "Core library for the qed stream editor"
license = "MIT"
repository = "https://github.com/boldlygodev/qed"

[dependencies]
regex = "1"
rayon = "1"
rand = "0.10"
uuid = { version = "1", features = ["v4", "v5", "v7"] }
